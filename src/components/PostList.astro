---
import type { CollectionEntry } from "astro:content";

const { posts, basePath } = Astro.props as {
  posts: CollectionEntry<any>[];
  basePath: string;
};
---

<ul>
  {posts.map(post => (
    <li>
      <a href={`/${basePath}/${post.slug}`}>
        <time>{post.data.date.toLocaleDateString("ja-JP", { year: "numeric", month: "2-digit", day: "2-digit", })}</time><br />
        {post.data.title}
      </a>
    </li>
  ))}
</ul>